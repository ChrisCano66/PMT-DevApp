import React from 'react';
export default React.createContext(null);

// création d'un context afin de passer plus facilement certaines informations dans l'application
// nottament le contenu des nouveaux items créer dans l'application
